framework:
    secret: '%env(APP_SECRET)%'

    # La session est activée proprement pour que le service session existe
    session:
        enabled: true

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file